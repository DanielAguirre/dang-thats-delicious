mixin loginForm()
  form.form(action='/login' method="POST")
    h2 Login
    label(for="email") Email Address
    input(type="email" name="email")
    label(for="passwor") Password
    input(type="password" name="passwor")
    input.button(type="submit" name="Log In ->")
    
