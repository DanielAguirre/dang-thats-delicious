extends layout

block content
  .inner
  h2= title
  .map
    .autocomplete
      input.autocomple__inpiut(type="text" placeholder="Search for Anything" name="geolocate")
    #map
      p Loading Map...
  
